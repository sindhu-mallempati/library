def sum(a,b)
{
   echo "The sum of ${a} and ${b} is ${a+b}"
 }
def sub(a,b)
{
   echo "The sub of ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
    echo "The mul is ${a} and ${b} is ${a*b}"
}
def divide(a,b)
{
    echo "The division is ${a} and ${b} is ${a/b}"
}
